# gosimple is provided by staticcheck in golangci-lint v2
version: "2"

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - errcheck
    - unused
    - ineffassign
    - gosec
    - cyclop
  settings:
    cyclop:
      max-complexity: 10
